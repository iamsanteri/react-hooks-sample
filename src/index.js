import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// Mounting the app into root of the index file, more details in App.js

ReactDOM.render(<App />, document.getElementById('root'));
