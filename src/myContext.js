import React from "react";

// This create context could also be declared directly in the App.js but outsourcing the state here should be explored in the future.
const myContext = React.createContext(null); 

export default myContext;